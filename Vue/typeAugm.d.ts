//type augmentation - so that plugin properties (e.g. socket) that are globally available to all component instances work well with TS

// import { Socket } from "socket.io-client";

// declare module "@vue/runtime-core" {
//   export interface ComponentCustomProperties {
//     $socket: Socket;
//   }
// }

// export {};
